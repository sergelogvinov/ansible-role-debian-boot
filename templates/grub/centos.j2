#!/bin/sh
exec tail -n +3 $0

menuentry "Debian Net Installer" {
    linux ($root)/debian-kernel {{ debian_cmdline }} {{ 'file=/debian-preseed.cfg' if debian_rebuild_initrd else debian_preseed_url }}
    initrd ($root)/debian-initrd{{ "-preseed" if debian_rebuild_initrd }}.gz
}
